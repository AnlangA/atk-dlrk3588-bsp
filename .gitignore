# Upstream sources and firmware blobs fetched by scripts/fetch.sh.
/external/
# Kernel, U-Boot, application and payload outputs.
/build/
# Host-specific toolchain paths and board address.
/local.env
# Board credentials and retained, customized Ubuntu recovery snapshots.
/local/
__pycache__/
